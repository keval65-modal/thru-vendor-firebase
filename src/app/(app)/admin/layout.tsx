// This is the dedicated layout for the /admin section.
// It does not use the AppShell, creating a separate environment.
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
